create a env and set up git with platform сделанно

check the project and try to go thougt the project

read papers!!! done 


Start project try to sync duild by the end of the day the agent which will work no fine-tuning today.
 1) get data for python !!  done
 2) start coding.
    describe nodes and build simple fix_agent (run through and see logs) !! done skeleton agent got buggy_code nad tests
    llm mode just copied and returred cndidate code(just buggy_code) and passed false log and no sandbox yet

Coding later:
    1)turn on LLM    done
    2)turn on real test run and sandbox for tests in Docker   done/ mot done
    3)ReAct (desicion node)  done
    4)Eval

Все вкрутил но пока все работает оч плохо завтра попробовать поиграться с моделями кодом промптами
 write etier system for correcting normaly works
 easy code running 
 search for models such that already good quality(explain why whese or that model) ps read papers
 ask for adviced about fine-tuning. Have 3 days 


    Finally some reasonable resylt of the React work after promt correction.
    next to try to test on subset of HumanEvalPack if not good get more data and fine-tune